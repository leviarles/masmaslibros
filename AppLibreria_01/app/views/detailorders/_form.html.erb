<%= form_for(@detailorder) do |f| %>
  <% if @detailorder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detailorder.errors.count, "error") %> prohibited this detailorder from being saved:</h2>

      <ul>
      <% @detailorder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.text_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad %>
  </div>
  <div class="field">
    <%= f.label :sale_id %><br>
    <%= f.text_field :sale_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
