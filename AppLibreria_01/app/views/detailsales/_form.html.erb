<%= form_for(@detailsale) do |f| %>
  <% if @detailsale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detailsale.errors.count, "error") %> prohibited this detailsale from being saved:</h2>

      <ul>
      <% @detailsale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.text_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad %>
  </div>
  <div class="field">
    <%= f.label :sale_id %><br>
    <%= f.text_field :sale_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
